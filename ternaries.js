// Ternaries Operators

const pages = 100;
pages > 1000 ? 'over a thousand' : 'less then 1000';  // will be left if true else will be right if false


const hasAdaptation = false

console.log(`The book has${hasAdaptation ? '' : ' not'} been adapted into a movie`) // Output: 'The book has not been adapted into a movie'

